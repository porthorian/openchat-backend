apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openchat-backend.fullname" . }}
  labels:
    {{- include "openchat-backend.labels" . | nindent 4 }}
spec:
  {{- $envMap := .Values.env | default dict }}
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
      {{- include "openchat-backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "openchat-backend.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "openchat-backend.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      containers:
        - name: openchatd
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ default .Chart.AppVersion .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.containerPort }}
              protocol: TCP
          env:
            {{- range $key, $val := $envMap }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            {{- if and .Values.coturn.enabled .Values.coturn.backend.injectEnv }}
            {{- if not (hasKey $envMap "OPENCHAT_RTC_TURN_URLS") }}
            - name: OPENCHAT_RTC_TURN_URLS
              value: {{ include "openchat-backend.coturn.requiredTurnURLsCSV" . | quote }}
            {{- end }}
            {{- if not (hasKey $envMap "OPENCHAT_RTC_STUN_URLS") }}
            - name: OPENCHAT_RTC_STUN_URLS
              value: {{ include "openchat-backend.coturn.requiredStunURLsCSV" . | quote }}
            {{- end }}
            {{- if not (hasKey $envMap "OPENCHAT_RTC_TURN_USERNAME") }}
            - name: OPENCHAT_RTC_TURN_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "openchat-backend.coturn.authSecretName" . }}
                  key: {{ .Values.coturn.auth.usernameKey }}
            {{- end }}
            {{- if not (hasKey $envMap "OPENCHAT_RTC_TURN_CREDENTIAL") }}
            - name: OPENCHAT_RTC_TURN_CREDENTIAL
              valueFrom:
                secretKeyRef:
                  name: {{ include "openchat-backend.coturn.authSecretName" . }}
                  key: {{ .Values.coturn.auth.passwordKey }}
            {{- end }}
            {{- if not (hasKey $envMap "OPENCHAT_RTC_TURN_CREDENTIAL_TYPE") }}
            - name: OPENCHAT_RTC_TURN_CREDENTIAL_TYPE
              value: "static"
            {{- end }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- if .Values.probes.liveness.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          {{- end }}
          {{- if .Values.probes.readiness.enabled }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
