apiVersion: v1
kind: Service
metadata:
  name: {{ include "openchat-backend.fullname" . }}
  labels:
    {{- include "openchat-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openchat-backend.selectorLabels" . | nindent 4 }}
